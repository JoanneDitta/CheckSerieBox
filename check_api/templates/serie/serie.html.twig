{# {% extends 'base.html.twig' %}

{% block title %}Hello SerieController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code>C:/Users/joann/Documents/GitHub/CheckSerieBox/check_api/src/Controller/SerieController.php</code></li>
        <li>Your template at <code>C:/Users/joann/Documents/GitHub/CheckSerieBox/check_api/templates/serie/index.html.twig</code></li>
    </ul>
</div>
{% endblock %} #}

{% extends 'base.html.twig' %}

{% block title %} {{ title }} {% endblock %}

{% block body %}
    <style>
    </style>

    <div class="d-flex justify-content-between gap-5">
        <img src={{ serie.posterUrl }} class=" border rounded" style="max-height: 300px" alt={{ serie.slug }} />
        <div>
            <div class="d-flex align-items-end gap-5 mb-5">
                <h1 class="m-0">{{ serie.title }}</h1>
                <p class="mb-1 link-underline-light">{{ serie.releaseDate }}</p>
            </div>
            <p>{{ serie.synopsis }}</p>
            <h3>Détails</h3>
            <table>
                <tr>
                    <td>Nombre de saison</td>
                    <td>{{ serie.nbSeason }}</td>
                </tr>
                <tr>
                    <td>Pays</td>
                    <td>{{ serie.country }}</td>
                </tr>
                <tr>
                    <td>Platforme</td>
                    <td>{{ serie.platform }}</td>
                </tr>
                <tr>
                    <td>Type</td>
                    <td>{{ serie.type }}</td>
                </tr>
                <tr>
                    <td>Status</td>
                    <td>{{ serie.status }}</td>
                </tr>
            </table>
        </div>
        <div class="bg-secondary  p-3 rounded">
            <div class="d-flex justify-content-between align-items-center gap-2">
                <img src={{ asset('Images/eye.svg') }} style="max-height: 35px" alt="seen" />
                <img src={{ asset('Images/like.svg') }} style="max-height: 40px" alt="like" />
                <img src={{ asset('Images/watchlist.svg') }} style="max-height: 40px" alt="watchlist" />
            </div>
        </div>
    </div>


<script>
</script>

{% endblock %}
