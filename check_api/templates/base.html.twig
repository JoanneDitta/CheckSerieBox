<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
        <style>
            input , select, textarea {
                border-radius: 5px;
                padding: 3px 5px 3px 10px;
            }
            select {
                width: 30%;
            }
            form {
                width: 50%;
                margin: 0 auto;
            }
            form div {
                padding: 5px 0 5px 0;
            }
        </style>
    </head>
    <body class="bg-dark text-white" style="min-height: 100vh">
        <header class="mb-5 px-5 py-2 bg-black d-flex justify-content-between align-items-center">
            <h1>CheckSerieBox</h1>
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <a href={{ path('app_logout') }} class="btn btn-danger text-white">Logout</a>
            {% else %}
                <div class="d-flex justify-content-between align-items-center gap-3">
                    <a href={{ path('app_login') }} class="btn btn-primary text-white">Login</a>
                    <a href={{ path('app_register') }} class="btn btn-primary text-white">Sign in</a>
                </div>
            {% endif %}
        </header>
        <main class="mx-5">
            {% block body %}
            {% endblock %}
        </main>
    </body>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</html>

